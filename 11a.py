from collections import namedtuple
from enum import Enum

# Position = namedtuple('Position', ['x', 'y'])

def does_space_expand():


with open('11ainput.txt', 'r') as f:
    universe = [list(line.strip()) for line in f]

